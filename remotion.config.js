/**
 * EOTC Voice Studio — Remotion Configuration
 * 
 * This configures the Remotion bundler for optimal rendering
 * of Amharic text and cinematic motion graphics.
 */
import { Config } from "@remotion/cli/config";

Config.setVideoImageFormat("jpeg");
Config.setOverwriteOutput(true);
